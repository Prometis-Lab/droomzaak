# Handover index

One line per handover, newest on top. Written by `/handover` (the `writing-handovers` skill).
Files live under `docs/handovers/<track>/<YYYY-MM-DD-HHMM>-<author>-<slug>.md` — per-track folders +
timestamp + author in the filename means several people can drop handovers at once without clobbering.

Tracks: `track-a-backend` (agent/tools/data) · `track-b-frontend` (canvas/UI) · `track-c-content` (pitch/narrative).

<!-- newest first; format: - [YYYY-MM-DD HH:MM] (track) author — slug → path -->

_(none yet — run `/handover` to add the first one)_
