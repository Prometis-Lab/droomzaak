# Semgrep scan scope — auto-discovered by semgrep/opengrep at the repo root.
# Keep the SAST gate focused on Droomzaak's OWN code. Third-party / generated
# code is out of scope: we don't own it and its findings would block /ship
# (PRD: the gate's exit code owns pass/fail — see .claude/hooks/security-gate.sh).

# Vendored third-party skills (Apache-2.0 — not our code; carries upstream findings)
.claude/skills/vendored/

# Optional local inspiration / prior implementations (gitignored)
reference/
inherited/

# Build-time ETL data scripts + test fixtures — not the agent's runtime query surface.
# They interpolate only internal Path constants into DuckDB SQL, read local Parquet, and
# write the warehouse. The loader (load_to_supabase.py) stays scanned, with justified
# inline `# nosemgrep` lines on its identifier-interpolation sites.
pipelines/droomzaak/_common.py
pipelines/droomzaak/datasets/
pipelines/droomzaak/tests/

# Dependencies & virtualenvs
node_modules/
.venv/
venv/
.uv/

# Build output & caches
dist/
build/
.cache/
__pycache__/
*.egg-info/

# Scanner artifacts
security.sarif
